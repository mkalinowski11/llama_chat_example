# models directory should contain quantized LLAMA models
* supported types: `.gguf`
* place model in the following directory, if a model has a different location,
change the model path env variable `MODEL_PATH` in `Dockerfile`